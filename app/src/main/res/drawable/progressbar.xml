<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/profile_progress_bg"
                android:centerColor="@color/profile_progress_bg"
                android:centerY="1.0"
                android:endColor="@color/profile_progress_bg"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/profile_progress_fg"
                    android:centerColor="@color/profile_progress_fg"
                    android:centerY="1.0"
                    android:endColor="@color/profile_progress_fg"
                    android:angle="270" />
            </shape>
        </clip>
    </item>
</layer-list> 